[package]
name = "stp_server"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
stp = { path = "../stp" }
smart_home = { path = "../smart_home" }
chrono = "0.4.38"
rand = "0.8.5"
tokio = { version = "1", features = ["full"] }